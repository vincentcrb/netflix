<div class="container col-5">

    <div class="row">
        <h1>Liste de films</h1>
        <a href="{{ path('add_movie') }}" class="btn btn-primary">Ajouter</a>
    </div>

    <div class="table-responsive col-12">
        <table class="table table-striped table-sm">
            <thead>
            <tr>
                <th>Id</th>
                <th colspan="3">Name</th>
            </tr>
            </thead>
            <tbody>
{% for movie in movies %}
<tr>
    <td>{{ movie.id }}</td>
    <td>{{ movie.name }}</td>
    <td><a href="{{ path('edit_movie', {'id': movie.id}) }}" class="btn btn-primary">Edit</a></td>
    <td><a href="{{ path('delete_movie', {'id': movie.id}) }}" class="btn btn-primary">Delete</a></td>
</tr>
{% endfor %}
            </tbody>
        </table>
    </div>
</div>
