<div class="container col-5">
<h1>Listes de films</h1>

    <div class="table-responsive col-12">
        <table class="table table-striped table-sm">
            <thead>
            <tr>
                <th>Id</th>
                <th colspan="2">Name</th>
            </tr>
            </thead>
            <tbody>
{% for movie in movies %}
<tr>
    <td>{{ movie.id }}</td>
    <td>{{ movie.name }}</td>
    <td><a href="{{ path('admin_profil_movie', {'id': movie.id}) }}" class="btn btn-primary">Edit</a></td>
</tr>
{% endfor %}
            </tbody>
        </table>
    </div>
</div>
