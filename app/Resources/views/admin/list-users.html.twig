<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Liste des users</title>
</head>
<body>
<h1>Liste des articles</h1>
{% for user in users %}
    <p>{{ user.username }}</p>
    <a href="{{ path('profil_user', {'id': user.id}) }}">view user</a>
{% endfor %}
<br/><br/>
<a href="{{ path('sign_out') }}">logout</a>
</body>
</html>